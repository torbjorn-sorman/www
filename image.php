<?php

function within_hour($e) {
    
}

function get_latest() {
    $n = glob('/uploads/*.*');
    sort($n);
    $tmp = array_filter($n, )
}

$last_path = $_GET["last"];

$n = array_slice($n, count($n))
if ($last_path == end($n))

return